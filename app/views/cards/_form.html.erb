<%= form_with(model: card, url: (@card.new_record? ? [@employee, @card]: @card), local: true) do |form| %>
  <% if card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(card.errors.count, "error") %> prohibited this card from being saved:</h2>

      <ul>
      <% card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :card_number %>
    <%= form.number_field :card_number, class: 'form-control' %>
  </div>

  <div class="field">
    <%= form.label :card_type %>
    <%= form.text_field :card_type, class: 'form-control' %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, :readonly => true, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit :class => 'btn btn-success' %>
  </div>
<% end %>
